// import { WeatherInfo } from "../components"
// import { connect } from "react-redux"
// import { getWeatherData } from "../services/actions/actions"

// const mapStateToProps = (state) => ({
//     weatherInfo: state.weatherReducer.weatherData,
//     tempInfo: state.weatherReducer.temp,
// })

// const mapDispatchToProps = (dispatch) => ({
//     getWeatherDataHandler: (data) => dispatch(getWeatherData(data))
// })

// export default connect(mapStateToProps, mapDispatchToProps)(WeatherInfo)